server.servlet.context-path=/vod

spring.datasource.url=[]
spring.datasource.username= postgresdev
spring.datasource.password= postgres-dev

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = create-drop

#aws
aws.accessKey=REPALCE_VALUE_prod
aws.secret=REPALCE_VALUE
aws.bucket=REPALCE_VALUE
aws.outputBucket=REPALCE_VALUE